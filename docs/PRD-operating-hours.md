# PRD: 운영시간 설정 기능

## 1. 개요

### 1.1 목적
리드 접수 시 고객에게 발송되는 SMS/이메일에 운영시간 정보를 포함하여 응답 예상 시간을 안내합니다.

### 1.2 범위
- 관리자 페이지: 운영시간 설정 UI
- 클라이언트 포털: 운영시간 설정 UI
- SMS/이메일 템플릿: 운영시간 변수 지원

---

## 2. 기능 요구사항

### 2.1 운영요일 설정

| 옵션 | 설명 | 표시 텍스트 |
|------|------|------------|
| weekdays | 주중 (월~금) | "평일 운영 (공휴일 휴무)" |
| everyday | 휴무없음 (월~일) | "연중무휴" |

### 2.2 운영시간 설정

| 필드 | 타입 | 기본값 | 예시 |
|------|------|--------|------|
| operatingStartTime | string | "09:00" | "10:00" |
| operatingEndTime | string | "18:00" | "20:00" |

### 2.3 데이터 스키마

```typescript
interface OperatingHours {
  operatingDays: 'weekdays' | 'everyday';  // 운영요일
  operatingStartTime: string;               // 시작시간 (HH:mm)
  operatingEndTime: string;                 // 종료시간 (HH:mm)
}
```

### 2.4 SMS/이메일 변수

| 변수 | 설명 | 예시 출력 |
|------|------|----------|
| {operatingHours} | 운영시간 전체 | "평일 09:00~18:00 (공휴일 휴무)" |
| {operatingDays} | 운영요일만 | "평일" 또는 "연중무휴" |
| {operatingTime} | 시간만 | "09:00~18:00" |

---

## 3. UI 설계

### 3.1 관리자/클라이언트 공통 UI

```
┌─────────────────────────────────────────────────────┐
│ 운영시간 설정                                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│ 운영요일                                            │
│ ○ 주중 (월~금)  ⚠️ 공휴일도 휴무입니다              │
│ ○ 연중무휴 (휴무없음)                               │
│                                                     │
│ 운영시간                                            │
│ [09:00 ▼] ~ [18:00 ▼]                              │
│                                                     │
│ 미리보기: "평일 09:00~18:00 (공휴일 휴무)"          │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 3.2 SMS 템플릿 예시

```
[업체명] {name}님, 상담 신청이 접수되었습니다.
운영시간: {operatingHours}
빠른 시일 내에 연락드리겠습니다.
```

### 3.3 이메일 표시

이메일 하단에 운영시간 정보 섹션 추가:
```
📞 운영시간 안내
평일 09:00~18:00 (공휴일 휴무)
```

---

## 4. API 변경사항

### 4.1 Airtable 필드 추가

| 필드명 | 타입 | 기본값 |
|--------|------|--------|
| operatingDays | Single Select | weekdays |
| operatingStartTime | Single line text | 09:00 |
| operatingEndTime | Single line text | 18:00 |

### 4.2 API 엔드포인트

- `GET /api/clients/[id]` - 운영시간 필드 포함
- `PUT /api/clients/[id]` - 운영시간 필드 업데이트
- `GET /api/portal/[slug]` - 운영시간 필드 포함
- `PUT /api/portal/[slug]` - 운영시간 필드 업데이트

---

## 5. 테스트 케이스

### 5.1 유틸리티 함수 테스트

```typescript
// formatOperatingHours 함수
describe('formatOperatingHours', () => {
  it('주중 + 기본시간 → "평일 09:00~18:00 (공휴일 휴무)"', () => {});
  it('연중무휴 + 기본시간 → "연중무휴 09:00~18:00"', () => {});
  it('주중 + 커스텀시간 → "평일 10:00~20:00 (공휴일 휴무)"', () => {});
  it('기본값 처리 → undefined일 때 기본값 사용', () => {});
});
```

### 5.2 API 테스트

```typescript
describe('운영시간 API', () => {
  it('클라이언트 생성 시 기본 운영시간 설정', () => {});
  it('운영시간 업데이트 성공', () => {});
  it('잘못된 시간 형식 거부', () => {});
});
```

---

## 6. 구현 순서

1. ✅ PRD 작성
2. 유틸리티 함수 테스트 작성 (TDD)
3. 유틸리티 함수 구현
4. Airtable/타입 정의 업데이트
5. 관리자 페이지 UI 추가
6. 클라이언트 포털 UI 추가
7. SMS/이메일 템플릿에 변수 적용

---

## 7. 완료 조건

- [ ] 운영시간 설정 UI (관리자/클라이언트)
- [ ] 운영시간 저장/조회 API
- [ ] SMS 템플릿에 {operatingHours} 변수 지원
- [ ] 이메일에 운영시간 정보 표시
- [ ] 테스트 통과
